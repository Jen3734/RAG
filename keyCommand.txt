
#remove the faiss_index

Remove-Item -Recurse -Force faiss_index
.\run_ragFAISS.bat




####thinking process######
1. first step is to accelerate the index building and searching process
    1. tune nlist and nprobe to fasten search index (check)
    2. tune the chunk size and chunk overlap to detail the search and precision the search (check)
    3. separate the doc into multiple segemnts and build index and search independently and in parallel through threads
2. second step is to enhance the reasoning capabilities
    1. idea 1: split the query in chunks to search unrelated chunks in mulitple vector store query and feed the query and the mutiple search result to llm to build relationships(check)
        1. how to constrain queries? There are too many duplicated sub-queries. modify the prompt to limit the subquerys and ask for no duplicated queries in meaning(check)
            1. next step would be to further consice the sub queries. some are still duplicated, but most sub queries various in meanings.